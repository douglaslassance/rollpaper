name: CD

on:
  # push:
  #   tags:
  #     - "*.*.*"
  workflow_dispatch:

jobs:
  release:
    uses: douglaslassance/catapult/.github/workflows/release.yml@37720adb22445a6d93fc31cf8c4332d934bd5ff9
    secrets: inherit
    with:
      channels: "direct,homebrew,appstore"
