name: CD

on:
  # push:
  #   tags:
  #     - "*.*.*"
  workflow_dispatch:

jobs:
  release:
    uses: douglaslassance/trigger/.github/workflows/release.yml@0.1.0
    secrets: inherit
    with:
      channels: "direct,homebrew,appstore"
