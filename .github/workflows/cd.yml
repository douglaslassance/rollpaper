name: CD

on:
  # push:
  #   tags:
  #     - "*.*.*"
  workflow_dispatch:

jobs:
  release:
    uses: douglaslassance/catapult/.github/workflows/release.yml@ce9443f76eb249f8de62fbca1c9aa52e7349ca87
    secrets: inherit
    with:
      channels: "s3,homebrew,appstore"
