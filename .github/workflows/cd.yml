name: CD

on:
  # push:
  #   tags:
  #     - "*.*.*"
  workflow_dispatch:

jobs:
  release:
    uses: douglaslassance/catapult/.github/workflows/release.yml@5480a2b9f431207253f908ffa4f31e4d164d1b24
    secrets: inherit
    with:
      channels: "s3,homebrew,appstore"
